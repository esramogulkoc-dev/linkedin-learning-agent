# LinkedIn Learning Journal Agent

This project is a small experiment to generate weekly LinkedIn learning journal posts using Google Gemini AI.

## Setup

1. Clone this repo.
2. Create a virtual environment (optional but recommended):
   ```bash
   python -m venv .venv
   source .venv/bin/activate  # macOS/Linux
   .venv\Scripts\activate     # Windows
